<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Editar Cliente</h1>
  <div>
    <%= link_to 'Ver', @client, class: 'btn btn-outline-primary' %>
    <%= link_to 'Voltar', clients_path, class: 'btn btn-secondary' %>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <%= render 'form' %>
  </div>
</div>