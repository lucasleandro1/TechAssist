<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <title>TechAssist - Busca de Dispositivos</title>
  </head>
  <body>
    <div class="container mt-5">
      <h1>Resultado da Busca - Dispositivos Móveis</h1>
      
      <%= link_to "← Voltar", root_path, class: "btn btn-secondary mb-3" %>
      
      <% if params[:q_imei_cont].present? %>
        <div class="alert alert-info">
          Resultados para IMEI: <strong><%= params[:q_imei_cont] %></strong>
        </div>
        
        <% if @mobile_devices && @mobile_devices.any? %>
          <div class="row">
            <% @mobile_devices.each do |device| %>
              <div class="col-md-6 mb-3">
                <div class="card">
                  <div class="card-body">
                    <h5 class="card-title"><%= device["brand"] rescue "Marca" %> <%= device["model"] rescue "Modelo" %></h5>
                    <p class="card-text">
                      <strong>IMEI:</strong> <%= device["imei"] rescue "N/A" %><br>
                      <strong>Marca:</strong> <%= device["brand"] rescue "N/A" %><br>
                      <strong>Modelo:</strong> <%= device["model"] rescue "N/A" %><br>
                      <strong>Status:</strong> <%= device["status"] rescue "N/A" %>
                    </p>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="alert alert-warning">
            Nenhum dispositivo encontrado com este IMEI.
          </div>
        <% end %>
      <% else %>
        <div class="alert alert-danger">
          Por favor, informe um IMEI para buscar.
        </div>
      <% end %>
      
      <!-- Formulário para nova busca -->
      <div class="mt-4">
        <h3>Nova Busca</h3>
        <%= form_with url: search_mobile_devices_path, method: :get, local: true do %>
          <div class="row">
            <div class="col-sm-8">
              <div class="form-group">
                <%= label_tag :q_imei_cont, "IMEI do Dispositivo" %>
                <%= text_field_tag :q_imei_cont, params[:q_imei_cont], class: "form-control", placeholder: "Digite o IMEI" %>
              </div>
            </div>
            <div class="col-sm-4">
              <%= submit_tag "Pesquisar", class: "btn btn-primary", style: "margin-top: 31px;" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>